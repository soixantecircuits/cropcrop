## Stocker le résultat d'une commande dans une variable

import subprocess                                          # Module pour exécuter des commandes dans le système
output = subprocess.check_output("ls", shell=True)         # Stocker sous forme texte dans output le resultat de la commande 


data = open("/home/sylvain/Scripts/Python/superbob").read()# Stocker le contenu d'un fichier dans une variable (sans plus, il le prend en lecture seule)


## Json
import json                                                # Module pour travailler le json
from pprint import pprint                                  # Module pour afficher joliment le json (pprint = pretty print
json_data=open('/home/sylvain/Scripts/Python/test.json')   # lire et stocker le fichier texte dans json_data
data = json.load(json_data)                                # transformer le fichier texte en json lisible par python
pprint(data)                                               # afficher joliment notre data pour être sur que tout va bien :D
json_data.close()                                          # ça marche, goodbye


## MediaInfo et Python
output = subprocess.check_output("mediainfo -f /home/sylvain/Vidéos/test.mp4", shell=True)

## Shell
mediainfo --Inform="Video;file://Video.txt" /home/sylvain/Vidéos/test.mp4
# Get Display aspect ratio
mediainfo --Inform="Video;%DisplayAspectRatio%" /home/sylvain/Vidéos/test.mp4
# Get Width
mediainfo --Inform="Video;%Width%" /home/sylvain/Vidéos/test.mp4
