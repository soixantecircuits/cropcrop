## Stocker le résultat d'une commande dans une variable
# Module pour exécuter des commandes dans le système
import subprocess
# Stocker sous forme texte dans output le resultat de la commande 
output = subprocess.check_output("ls", shell=True)

## Stocker le contenu d'un fichier dans une variable (sans plus, il le prend en lecture seule)
data = open("/home/sylvain/Scripts/Python/superbob").read()

## Json
# Module pour travailler le json
import json
# Module pour afficher joliment le json (pprint = pretty print
from pprint import pprint
# lire et stocker le fichier texte dans json_data
json_data=open('/home/sylvain/Scripts/Python/test.json')
# transformer le fichier texte en json lisible par python
data = json.load(json_data)
# afficher joliment notre data pour être sur que tout va bien :D
pprint(data)
# ça marche, goodbye
json_data.close()


# chemins
/home/sylvain/Scripts/Python/superbob
/home/sylvain/Scripts/Python/test.json
/home/sylvain/Vidéos/test.mp4

## MediaInfo et Python
output = subprocess.check_output("mediainfo -f /home/sylvain/Vidéos/test.mp4", shell=True)
#NE MARCHE PAS  output = subprocess.check_output("mediainfo -f /home/sylvain/Vidéos/test.mp4", shell=True).sub("\n", "", handle)

## Shell
mediainfo --Inform="Video;file://Video.txt" /home/sylvain/Vidéos/test.mp4
# Get Display aspect ratio
mediainfo --Inform="Video;%DisplayAspectRatio%" /home/sylvain/Vidéos/test.mp4
# Get Width
mediainfo --Inform="Video;%Width%" /home/sylvain/Vidéos/test.mp4
